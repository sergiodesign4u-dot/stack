# Stack

A mobile-first sport nutrition store for the Ukrainian market, built around coaches and gyms who
order for their athletes. Turns a confusing catalog into a clear, trusted path from a person's goal
to the right products.

**Entry point:** https://sergiodesign4u-dot.github.io/stack/ - the project route with every stage and
its status. Every page carries the same left sidebar, rendered from one registry (`/_nav.js`), so
there is no page list to maintain here.

## Status

| # | Stage | Status |
|---|-------|--------|
| 01 | Foundation Research | Done |
| 02 | User Research (personas + JTBD) | Done |
| 02+ | CJM (As-Is + To-Be) | Done - Olena x main job, emotions sourced live; added the order status notification to the MVP |
| 03 | Information architecture | Done - base layer, detail layer, node hub and block bank |
| 04 | Wireframes | Done - grey clickable prototype, 141 screens + hub |
| 05 | Voice | Done - rulebook + microcopy inventory, rolled out to the prototype |
| 06 | Concept | Done - visual language on two screens |
| 07 | UI + Visual | Done - component kit and inventory; the coloured layer is 91 screens of 142 plus the stage hub, counted 2026-08-19 (46 buyer + 41 coach; A13 brought 8 at step 7.95, step 8.7 added three, step 8.13 two, and step 8.14 the 35 page states the grey layer already drew) |
| 08 | Tokens + Components | Done - **84 component files, and every one of them carries a stand page**; two token levels with a dark theme written over the semantic block; `kit.css` deleted at step 8 and the sample moved onto `system/index.css` with a pixel proof. Step 9 closed it: `/impeccable audit` ran, and the dry run over 54 grey screens with no coloured twin found **228 classes in 79 families with no component**, of which exactly one family crosses more than two screens - `info-*`, the content page, on six. Recorded as A19 and deferred on purpose: those six screens are still grey, so a component built now would be worn by nobody. Why any of it is the way it is: `docs/decisions.md`. |
| 09 | Design System | Done - **one pattern, nine rules of use, and a proof that the pattern moved nothing**. The product was repeating one composition by hand under **53 container names**; `patterns/action-row.css` now holds it - 4 rules, 3 classes, **zero colour declarations of its own**, imported after the components. 13 container names in 10 component files were **cut out**, not copied, and the 15 coloured screens wearing `actions` were converted with **0.000% pixel movement on all 15** (`proof.mjs --against`, working tree vs `c07e2c8`, both halves shot live in one browser). The same counter run backwards gave the prohibitions: **U1-U9 in `architecture.md` section I**, each with its «where it came from» filled, echoed as an «Обмеження» sub-item on 27 component pages. `why.html` is the stage's roadmap page. The self-sufficiency test built a real product node (`goal` 2.2 plus three states) with **no private styles at all** and a backlog that is not empty - 6 gaps, one of them a missing IA screen. **Two token rungs were added on the way** (`--warm-400` `#AA9D8A`, `--warm-500` `#9A8B73`), because dark secondary text had no rung. Step 6 ran two instruments plus the contract as a third: **28 findings confirmed, 5 withdrawn on verification and left visible**, a second Codex pass over the repaired files that caught the repair three times, and **four instruments that had been answering a false zero** - `links.mjs` had never read a `src`, `proof.mjs` could not prove a screen without a stored baseline, `inventory.mjs` never asked its own summaries, and nothing at all asked whether a path NAMED in an md still exists (`tools/paths.mjs`, new). Open and waiting on the owner, listed on `pixel-proof.html`: the dark-theme contrast hole on `.hptag`, 61 anatomy captions naming a zone without a class, and the 43 screens the composition stands on but has not been converted to - which is stage 12. |
| 10 | Responsive | **Done.** **The census found 36 raw width values, 27 after mirroring, 18 acting in the product, and not one token for any of them**; `@container`, `container-type`, `clamp()` and `rem`-in-a-media-query were all at zero while 53 of the 88 files in `design/system/` already carried an `@media`. Step 2 wrote **two points in `rem`, named by the change and not by a device** (`--bp-grid-2col` 38.75rem, `--bp-shell-wide` 53.75rem), plus `--container-page`, `--container-text` in `ch` and two grid floors, and moved the type ramp from `px` to `rem` value for value. Step 3 took the shell fork: **form A**, chosen by the owner and already in the code - measured with the new `tools/tab-walk.mjs`, which presses a real Tab: at 1280 exactly **one** navigation carrier, focus on an invisible element **0**. Step 4 round 1 closed the atoms: **N = 23, M = 9, K = 14**, four off-registry numbers gone (479 twice, 1040, and 639 moved to the file that owns it), and the skeleton grid was found promising **four columns of 185px where the real grid gives three of 248** - both now agree to the pixel. Measured across all 92 coloured pages: **0 boxes moved at 360**. The page is `design/kit/responsive.html`. **The registry of components was rebuilt at step 4 because the first measurement was wrong three times over** - it asked the frozen grey corpus, then read `class="..."` statically, then took every class token instead of only the ones a single file owns; `tools/comp-width.mjs` carries all three wrong versions. Step 4 round 2 closed the molecules: **N = 27, M = 23, K = 4**, and the level went from **37 queries over 14 widths to 30 over 11** - every off-registry number left stands in the two files held on three owner questions (`trust-strip`, `seo-text`). The round's own roll-call was overruled by measurement in four places: `auto-fit` fills a row and cannot express the balanced arrangements of a FIXED item count, so six goal tiles, six brand logos and three blog cards keep a point. It also found a defect nothing had asked about - the restock row's e-mail field was **26 pixels wide from 420 all the way to 1600**, hidden by the very query that was defending its own boundary. New instrument `tools/grid-sweep.mjs` asks the resolved track list at every width from 320 to 1600; it was wrong twice first, and both versions are in its header. Measured again across all 92 coloured pages: **0 boxes moved at 360**. Step 4 round 3 closed the organisms, the heaviest level: **N = 34, M = 32, K = 2**, and the level fell from **86 queries over 19 widths to 80 over 7** - across all of `design/system/` it is now **117 queries on 13 widths**, with every off-registry number confined to four deliberately held files. Its largest finding is that `product-grid` carried **two queries that had never painted** (the stage-08 colour half out-declares them, so the census had counted 1040 as an acting boundary when it acts on nothing) over a grid that **lost a column as the window grew** - 3 columns at 520, 2 at 620 - because the floor switched 150 to 200 at the point. The switch became a ramp. The account proved to be a **second shell** and folded onto `--bp-shell-wide` by default, since a third point needs the owner to name it. Measured: **0 differences in 184 comparisons** at 360 and 1280, which is the expected result - every folded number lies between the anchors, where only the width sweep can see it. Step 4 round 4 closed the patterns - **N = 1, M = 1, K = 0**, the one pattern holds no query at all - and resolved the four held files. **The whole of `design/system/` was then 116 queries on SIX widths**: 619, 620, 859, 860 and the pair 939/940 that step 5 owned, against the **27 different width values with no token** the census found at step 1. The mascot folded cleanly beside the SEO text and BROKE beside the trust strip (four cells of 167, two clipping), so its reserve and its crop box became ramps. New instrument `tools/bp.mjs` reads the registry out of `tokens.css` and fails on four silent classes; all four were proved by being introduced and reverted. The `@media`-in-a-screen-file ban is written in `design/system/CLAUDE.md` (rules 11 and 12) and `architecture.md` section J, and the `Width` column now covers all 84 inventory rows, derived rather than typed. Step 5 built the one behaviour the audit named and nothing else: **the coach split view, both pairs**. The threshold is the pack's own - two or more list-and-detail pairs, or one pair of the main flow - and both hold: the saved clients list beside one client's record, and the session strip beside that client's basket. The detail screen is **not** cancelled (`coach-client.html` keeps its URL, breadcrumbs and SEO block), the panel opens **empty** on purpose with six new rows in `microcopy.md`, and the frame and the panel have **one edition** - `wfClientSplit()` builds them, so a screen states nothing about width and the rule falls out on its own: **no list means no split** (`empty` and `error` have no cards; `loading` does get the frame, or the page would jump at 860 the moment data arrives). Moving the client strip inside `.cs-grid` on all eight session screens spent **939/940**, the last pair deliberately off the registry - and `tools/bp.mjs` immediately **failed**, because its `EXCUSED` list no longer covered anything, which is the idle control working. **`design/system/` is now 117 queries on FOUR widths: 619, 620, 859, 860, and nothing else.** New instrument `tools/split.mjs` asks `getBoundingClientRect` whether the frame really has two columns and whether the two panes really sit in them, and rolls the whole corpus for a page that carries the list and stands outside a frame; five failure classes, each proved by being introduced on purpose, two wrong versions in its header. Two things the instruments could not find: `.cldetail-empty` was **`.emptybox.mini` written a second time** inside another component's file, so `.et` and `.es` matched nothing and heading and body rendered at the same size - found by opening the page, because no instrument asks whether a heading looks like a heading; and **22 of the 84 component stand pages describe a width their own file no longer holds**, of which step 5 repaired the two it touched. One question is open for the owner and costed in `backlog.md`: at the point the detail pane is **224px**, narrower than the rail, because the account shell takes its own 268px column - `@media` is answering about the SCREEN when the frame needs the PLACE. Measured: **282 screens, 0 failures at 390 and at 360**. **`/impeccable critique` then ran as two isolated agents and scored the result 24/40**, and its verdict was not a defect list: the specificity was SPLIT and the halves inverted - the session pair is authored for the coach channel and got layout only, the clients pair is a contact list with the labels changed and got all the new interaction engineering. Every repair follows from that. The panel's rows were Ціль / Телефон / E-mail while the card above it carried «8 замовлень · останнє 12 черв. 2026» - **the detail was less domain-relevant than the summary it detailed**; rows are now Ціль · Замовлень · Останнє замовлення with the coach's note promoted into a marked strip. Two names per destination collapsed to one, and `?client=` - the contract this step invented and then dropped at every action - now travels with both the panel's buttons and each card's own links. The card stopped claiming `role="button"` while containing two links; the claim moved onto the client's NAME. The session strip claimed `role="tablist"` with **no handler anywhere** and is now the same selection model the clients rail uses. Both new controls declare `--ring-focus-control` instead of falling through to Chrome's blue. **And the backlog's open question was closed by taking the ladder's own answer**: `container-type` on `.acc-main` plus `@container (min-width: 41rem)` on the frame, where 41rem is derived rather than chosen - rail + gap + the smallest panel worth opening. The split now opens at a **990** viewport with a **354px** pane; the 224px pane is gone. The first version of that was WRONG and the instrument caught it in one run: a bare `@container` turned the split on below 860 and off between 860 and 960, because **the place is not monotonic in the viewport**. Two gates in rank order fixed it. `tools/split.mjs` was rebuilt to declare each frame's RULE and sweep 320-1600 at 10px rather than assert where the point is - its third wrong version, written in its header. Four session screens clip between 320 and 350 and a `git stash` against the pre-step-5 tree proved every one identical on the baseline. **Two owner debts then closed by decision.** The header search collapsed to 44px between the shell point and ~1010; the owner chose to let the action labels go, and it became a CONTAINER question rather than a point - the ROW runs out of room and its width is not the window's, so `container-type` on `.wfh-main` with the words going at `@container (max-width: 63rem)`, a threshold that is the sum the row must hold with them in. The words are hidden from the eye, not the reader: `display: none` would have stripped the accessible name off four controls whose only text this is. **Half closed, and the estimate was wrong about the other half**: the actions give back 44px, not the predicted 408, because the buttons keep icons, padding and badges - the field goes 44 → 88 at the point and the words return at 1040. And the bonus action wore a `star` while the three actions beside it named their job, because the 67-glyph set had no mark for MONEY; the owner chose a new `coin`, two concentric circles at the set's own stroke, which reached the stand automatically because `icons.html` renders the set live. One new debt opened and is recorded: `header.css` now repeats the five visually-hidden declarations `menu.css` already carries. **Then a debts pass, and three of its four findings were about the CHECKS.** The dark-theme hole is closed and the backlog's diagnosis was wrong about the mechanism: not `--bg-page` in `banner.css` but `--bg-inverse` in `hero.css` - «inverse» means «opposite of the page» so the whole family flips, and `--text-action` does not flip with it. The owner chose a ground of its own: a new semantic family whose **two halves are deliberately equal** (`--bg-media` and its three companions), argued on the grounds that a promo panel is a PICTURE surface and the two tiles beside it carry photographs, which do not learn a theme. `theme.mjs` re-run: **35 forms with 1 broken by the theme → 28 with 0**. The 20 stand pages describing widths their files no longer hold are closed, and **11 of the 49 ghost numbers were legal** - 8 pages name history, 3 style their own demo tables, and the first measurement counted those wrongly. Nine pages carried a stale CURRENT claim and were rebuilt; the «У файлі N медіа-умов» lists are now GENERATED from the file rather than typed. **And it is checked now**: `inventory.mjs` gained class H2 with both legal classes declared and idle-controlled, both failure modes proved by introduction. `tools/vars.mjs` then caught the hover added an hour earlier to fix a missing hover state: it reached for `--bg-hover`, **a token declared nowhere**, so the declaration was invalid at computed-value time and the hover drew nothing - fixed by reusing `--bg-sunken`, which every other hovered row already takes. «Обране» duplicated across two carriers was WITHDRAWN on verification - at 360 there is exactly one visible carrier. The search placeholder shortens on the narrow row by asking the OUTPUT rather than copying the 63rem threshold. **Step 6 then ran the critique on three instruments** - Claude with the apparatus, Codex read-only over the source, and `/impeccable audit` as an isolated browser agent - plus the contract as a fourth, and its verdict was that **five of the six document defects had been made by the two passes immediately before it**. `roles.mjs` and `inventory.mjs` failed on files the debts pass had edited an hour earlier; `CLAUDE.md` already says a repair stales its neighbours, and what it did not say is that the instrument catching that must be re-run AFTER the repair. The same loop closed four more times inside step 6 and each took a minute rather than two stages. **The stage's own audit table had gone stale about its own delivery in both directions**: 11 rows still read «NEW BEHAVIOUR · MISSING» for behaviour step 5 built, and two screens that DO carry the frame sat in «THE SAME» - corrected by measurement (`.cs-grid` gives `320px 692px` at 1280 on the empty session states too), and the count of eleven stayed while the membership changed. **The largest finding was in no source file at all: of 80 visible focusables on `coach-clients` at 1280, 51 drew Chrome's own `rgb(0,95,204)`**, a fixed value that does not follow the dark theme. A floor in `base.css` now takes the same `--ring-focus-control`; its first writing was one specificity step too low and left 20 home-screen links with no ring at all, which is worse than the blue it replaced. **Four resets were deleting the ring permanently** - `box-shadow: none` on a control at (0,2,0) in a file loading after `button.css` ties and wins on source order, a defect that exists only in the resolved output. **And the accessible name of every product link was the word «фото»** - 222 of them on 35 of 92 pages, against 28 `<img>` in the folder; one edition in `design/system/marks.js` reads the name off the card, and the first edition was in the wrong file, which `accept.mjs` said within a minute. Values that moved, said out loud: `--size-62` REMOVED (zero readers, found three times without being acted on), 58 declarations `px` to `rem` including ten `max-width: 1200px` where `--container-page` already existed (`proof.mjs --against HEAD`: nothing moved on 40 screens), and the reading measure given five readers where running text had reached 149.8ch at 1600. Four new or repaired instruments: `dupe.mjs`, `typo.mjs`, `focus.mjs`, and `dead-sel.mjs` taught that `:has()` is not `:not()`. Gates: accept 282/0 at 390 and 360 · focus 19 071 controls, 0 UA blue, 0 ringless · split · theme · bp · vars · links · paths · inventory · dupe · typo · roles · dead-sel all clean. **Step 7 closed the deferred sweep, and it was the stage's own subject.** `responsive.md` had one row red on purpose - «breaks between the points, not measured» - because `accept.mjs` walks 360 and 390, `tree-diff.mjs` compares two trees, `grid-sweep.mjs` counts columns and `split.mjs` sweeps 129 widths **only for the frames it declares**. None asks an arbitrary element about an arbitrary width, so a stage whose whole subject is width closed with its main class carrying no findings. `tools/width-sweep.mjs` asks four questions at every width from 320 to 1600 and bisects each crossing to the pixel: **32 defects above the floor on 91 pages, all repaired, all re-checked by the same instrument.** The one no other instrument here could have seen: `.ob-side` on `account-orders` renders **56.8px past the edge between 1076 and ~1160 with `scrollWidth === clientWidth`** - an ancestor clips it, so the panel is not pushed somewhere findable, it is cut off in silence; 1060 is clean and 1280 is clean, which is why two viewports missed it and 129 did not. The cause was a question asked of the wrong thing - `1fr 292px` under a VIEWPORT query, while what runs out of room is the COLUMN - so the repair is the **fourth container threshold**, `36.5rem` on `.acc-main`, and the number is the sum `292 + 24 + 268` rather than a device. `bp.mjs` failed the run until the row was in the registry. **The reading measure went 5 to 31**: stage 10 measured prose at 1600 alone, and prose crosses the measure not where the window is widest but where its own column stops sharing the row. `.tsb` runs to **132.1ch**, nearly twice the ceiling, and was invisible behind `.qans` until the instrument stopped reporting one offender per width. **Two of the first twenty caps missed and the sweep is what said so** - `.coach .crestock .rk-lead` did not cover the buyer cabinet, `.cd-oosnote .m` is a different `.m`; both named in the css rather than quietly widened. And one finding was about a KIND of number: `.pf-note` was capped at `38.75rem`, which is `--bp-grid-2col` - a **breakpoint used as a reading measure**. **Twelve wrong versions of the instrument**, four of which changed what it can do: the measure was taken of the BOX and the measure is about the LINE (`.qans` read 132.3ch on a twenty-eight-character sentence, found by opening all 21 findings by hand); one offender per width means a fix reveals the next, and with one boolean per class anything true from below the floor reported nothing above it; a rail is not a break (14 of 15); the inside of an `<svg>` is not layout. Gates after: width-sweep 0/0/0/0 · `bp.mjs` 4 container thresholds in system and registry · `accept` 283/0 · `proof.mjs --against HEAD` **39 of 40 screens moved nothing**, the one difference being the loading screen already recorded as non-reproducible · typo. `wireframes/` unchanged. |
| 11 | Animation | **Done.** **The census was taken from both ends and the product turned out to hold 12 different durations and no chosen curve at all**: `.15s` on 92 declarations, then `.14s` `.12s` `.22s` `.18s` `.16s` `.2s` `.25s` `.28s` `.9s` `1.1s` and a `0s`, with `ease` on **817 of 818 resolved timing functions** - the value a declaration gets when nobody names one. `tools/motion.mjs` asks the question twice, because the two ends disagree: `--source` reads every stylesheet and can name a FILE, `--output` resolves 279 pages in Chrome and can name a NUMBER that is true. **Six wrong versions are in its header**, two of them the same fault in different halves - it counted time values written inside comments, it asked only the focusable elements and so was blind to STATUS and CONNECTION entirely, it took every other time in a shorthand as a duration, and its list of state selectors was typed from habit: it asked about `[aria-selected]` and never `[aria-current]`, so `tabbar.css`, whose whole job is to mark the page you stand on, was reported as having no state. The stand's zero was proved by introduction. **The like-for-like comparison is the number the stage exists for**: thorne.com, the benchmark leader of `research.md`, runs its home page on **2 durations and one chosen curve on 95 of 110 elements**; `design/index.html` ran on **8 durations** and `ease`. The inventory of moments carries a verdict on **all 85 rows** of `inventory.md`, from two corpora that are not interchangeable - a flow map never names the hover of a button, so an inventory taken from screens sees RESPONSE almost not at all. **Organism is the level named out loud**: 34 components, 24 with a state, 13 moving, and the eleven with a state and no motion are exactly the surfaces, the client panel of step 10.5 among them. `favourite.css`, the heart on the card at 92 instances, has a focus state and does not move; `icon.css` moves and has no state, the only confetti in the corpus. **And the structural finding outranks the numbers: every surface in this product appears by switching `display`** - 11 files, 16 declarations - while `@starting-style` and `allow-discrete` are at zero, so an ordinary transition cannot animate any of it. That is step 4's decision, beside the between-documents fork. **The reference source was substituted and the substitution is recorded, not silent**: `motionsites` returned `free_prompts_remaining: 0` and the owner replaced it with `greensock/gsap-skills`, read live. GSAP does not ship: the technique is taken, not the library, and the source itself states that its CustomEase uses CSS `cubic-bezier()` values. **Step 2 wrote three durations, three curves and two distances, all on the primitive level and none of them with a theme pair** - 150ms is the product's own majority taken rather than replaced, 220ms is a value it already held with its JOB changed, and 330ms is the one number the census did not contain, because the whole product lives inside a 160ms span holding no rung that reads as arrival. The curves were **solved, not recalled** (`tools/ease-fit.mjs`, 0.19-0.36% max error), and `back.out` and `elastic` are refused by name on `voice.md`'s authority. The `prefers-reduced-motion` block redefines those same tokens in the same pass, and step 2 wrote that it stood **with no safety net on `*`** - **which was false, and step 5 corrected it**: `base.css` had carried `*{ transition: none !important }` under `reduce` since stage 07, and `!important` on `*` beats every declaration in `tokens.css`. The page is `design/kit/motion.html`, and its examples are live because motion is the one artifact a screenshot cannot carry. **One question is open for the owner: the STATUS cycle has no token** - `skeleton.css` carries `1.1s` and two spinners `.9s` as literals, and no rung of three can express a cycle. **Step 3 ran the ladder in four rounds and closed each by number**: atoms 23 = 16 + 7, molecules 27 = 19 + 8, organisms 34 = 22 + 12, the pattern 1 = 1 + 0. **The product went from 12 different durations to four, and three of the four are the cycles**; `transition: all` went from one declaration reaching 32 elements to **zero**, and `ease` - the curve nobody chose - from 131 occurrences to one, on a page that does not load the system. The proof is a **token swap**, not the reduced-motion emulation the method assumes: under that emulation everything came back zero, so the check passed on `product-card`, which carries a bare `.18s` and reads no token at all. **The cause named here was wrong and step 5 corrected it** - it was not Chrome zeroing anything, it was this repository's own `*{ transition: none !important }` in `base.css`, which `cdp.mjs` had been naming in a comment since stage 07. Overriding `--dur-*` with three values that appear nowhere in the product separates the two with no ambiguity. **Every round found a blind spot in the instrument rather than in the product, and each was found by pointing it at a case that had to fail**: a declaration spanning two lines converted by half in silence (grep saw nothing, the swap saw `7.77s, 7.77s, 7.77s, 0.15s, 0.15s`); the coach stands rendering their component inside a same-origin iframe the walk never entered; motion declared on `::before`, invisible because the scope strips the pseudo part; `price-slider`, whose motion lives on a vendor pseudo-element `getComputedStyle` cannot read at all, now reported as a named limit rather than as «does not move»; `0s` counted as drift when it is a decision; and the curve counter matching `ease` inside `var(--ease-standard)`, so the number went UP while the corpus got tidier. **The two claims step 1 made about the corpus were both wrong** and the roll-call caught both: `icon.css` is not confetti (its chevron is turned by `order-row` and `trust-strip`, a state in one file and its motion in another), and the heart at 92 instances does move (its transition comes from `button.css`). Two more instruments caught staleness nothing else asked about: `roles.mjs` found 43 stand pages not listing the two new primitives, and `inventory.mjs` found `motion.html` had no card in the kit hub. **`design/overview.html` keeps three transitions as a declared exception** - it is the stage hub, does not load `system/index.css` and declares its own colour vocabulary, so a token would not resolve there; it is already the declared exception for the `@media` ban. **Step 4 opened on four owner decisions and closed all four.** The cycle got a FOURTH PAIR rather than a fourth rung - `--dur-cycle: 1100ms`, `--ease-cycle: linear` - because a transition has an end and a person waiting for it while a cycle has neither, so comparing 1100 with 330 would be comparing different things. The number came from the census counted **by instance rather than by declaration, and the two disagree**: by declaration the spinners win 2 to 1, by instance the pulse wins 22 grey and 39 coloured against 3. Each spinner now turns 200ms slower and nobody can put a word to that; the other way round costs the pulse 18% more agitation on the one screen a person stares at while waiting, and `voice.md` puts the wait after paying at target emotion ZERO. `linear` is both the census majority and the only curve a rotation can take, and **what the single curve costs is paid where it is felt**: the pulse's softness moved into its keyframes (seven stops approximating a cosine, same end values) rather than into a second curve token existing for one selector. Neither cycle token is in the `reduce` block, and the absence is the decision: a transition at 1ms is over before it is seen, a cycle at 1ms repeats a thousand times a second. **The second decision was the biggest: 20 surfaces appear by switching `display`**, and `display` is discrete - no midpoint, so a transition has nothing to interpolate, and a component could read `--dur-slow` honestly, pass the roll-call of step 3 and still arrive in one frame. All 20 now carry `transition-behavior: allow-discrete` beside `@starting-style`, plus a 21st the instrument cannot see - the split view's panel, switched from javascript through `hidden`, counted by hand and printed as such so 20 is never read as «all». `tools/motion.mjs --surfaces` found **three defects in itself before it found any in the product**, and the first is the one worth keeping: it assumed the state marker stands on the VISIBLE side, so `.wf-cookie.hidden` - the surface every visitor meets - was missing from the list, and the count went 19 to 20. `--surfaces --live` then asks the browser, because the source cannot answer it: `allow-discrete` without a matching `@starting-style` parses, passes every source check and still jumps. **Support was measured, not recalled** - Chrome 151 parses all four features; Safari and Firefox were not measured in this session, and that is written down rather than assumed. **The third decision went against the recommendation and is recorded that way**: branch A was recommended on the pack's own rule (branch B is taken «only if the inventory holds a concrete pair of screens with CONNECTION», and it holds none), the owner took **B** - one `@view-transition { navigation: auto }` in `base.css` - so this is the one moment in the stage that came from a decision rather than from the corpus, and it pays with a row of its own and a critique step 6 owes. Its `reduce` is closed by name, because the browser's crossfade is an animation and no token override reaches it. **The fourth: no motion at a breakpoint, exception included** - the rail and the split panel qualify for the pack's exception on paper, but all three arrive ONLY through a media query, that is from a window being resized rather than from anything a person did, so there is nothing to announce. Nothing was written into any file for that, which is the point of recording it. **The tone check ran per state against four values each and found one animation defect**: `wfToast('error', ...)` arrived on the same 330ms and the same ten pixels as «Адресу збережено», one movement under two opposite sentences. It now takes `--dur-base` and `--move-sm` through a local alias, not a redefined token - the first writing set `--move-md: var(--move-sm)`, which works and reads as a lie, and `roles.mjs` promptly reported `--move-md` as unused. **It also found an animation being cut by a file this stage may not touch**: `wireframes/_nav.js` removes the toast node 250ms after `.out`, and step 3 had put the exit on 330, so the toast vanished 80ms before its own fade finished and no css census could see it. The grey corpus is frozen, so the duration moved: 220 fits inside 250. **And one discrepancy turned out to be in the text**: `microcopy.md` carried «Вітаємо у Stack! Ви увійшли 🎉» while the product says «Ви увійшли» - the product's edition is the one that obeys Principle 5, and no product string exists in two editions. **Patterns: one, and it gets no transition of its own**, said with the number and the reason - a row of actions never appears alone, it arrives with the block that holds it, and giving it an entrance would animate a moment for which none of the three jobs can be named. The split view instead is the cleanest CONNECTION in the product: the detail comes from the side of the list, `--move-md` left, `--dur-base`, **and with no fade at all** - step 10.5 moves focus to the client's name in the same instant, and a panel starting at zero opacity would have a screen reader reading what the eye cannot yet see. **An eighth wrong version surfaced in the census and it was invisible until the product changed**: `opacity` stood in the expensive-paint list AND in the cheap list at once, so the moment twenty surfaces got an opacity transition, «animates something expensive» jumped from 73 to 107 with every new case being the cheapest thing in css - step 5's frame-cost table would have been built on it. Two new instruments (`--surfaces`, `--surfaces --live`) and one new repair (`tools/motion-row.mjs`, which rebuilds a stand page's motion row from its own css instead of fifteen hand edits). **`inventory.mjs --apply` then closed a debt that predates this stage**: the Lines column and 64 stand meta numbers, including the 26 that were standing red before Animation opened. Gates: accept 283/0 at 390 · roles 84/0 · inventory Lines 0, meta 0 · vars 283/0 · links 5353/0 · paths 1406/0 · typo · css-comments all clean. **Step 5 opened by finding that the biggest claim of steps 2 and 3 was false.** `base.css` had carried `@media (prefers-reduced-motion: reduce){ *{ transition: none !important } }` since stage 07, while step 2 recorded that the system deliberately stood **with no safety net on `*`** - a description of the intention, not of the tree. `!important` on `*` beats every declaration in `tokens.css`, so the audit this step exists for would have come back **green over an empty field**, and the same line had already put a wrong cause into the record of step 3, where the zeros the emulation returned were blamed on Chrome. Chrome does no such thing; `cdp.mjs` had been naming the real cause in a comment for two stages. Removed before the audit was first run - **which is what turned the audit red: 96 elements against 0** - and corrected in five places. **The frame cost: three of the four layout animations converted, the fourth cannot be.** All three were switch knobs riding on `left`, and in each the two numbers the file carried were one fact written twice: the knob rests an inset from its own end of the track and crosses what is left, so `44 - 20 - 3*2 = 18` is exactly `21 - 3`. Measured to the pixel (21 -> 3 travel **-18**; 2 -> 18 travel **+16**) and `proof.mjs --against HEAD` gives **0.000% of pixels moved**; the third, `.ck-tog`, **cannot be proved at all and that is said rather than skipped** - the cookie banner stands on ONE page in the whole tree, in the frozen corpus, so its coloured file draws nothing today. The header's `max-height` **stays**, with the browser's own numbers behind the decision: 0 layouts idle against **25 layouts, 1.1ms layout, 1.3ms style** for the collapse, 1 layout for the same element on `opacity` - and the meta bar exists only from 860px, so the weak phone the method worries about never renders it. **Expensive paint is 24 declarations, not the 70 the census prints** (46 are `color` and `background` on hovers), and **the list of twenty cards the method fears does not occur here**: every one of the 24 triggers is a pointer, focus or a selection class, and there is one of each. **The place where half the document does move at once is the theme switch** - 463 of `listing.html`'s 2006 elements carry a colour transition, and flipping `[data-theme]` starts all of them for **32.1ms of style recalculation**, which nobody ordered: those rules were written for a pointer. The owner chose to suppress it, and `theme.js` now holds a shutter for exactly two animation frames (the first callback runs before the change is painted, so removing the class there would let the transitions start anyway). Proved through the product's own entry point rather than by setting the attribute by hand, **which is what the first measurement did and why it saw no change**: through `uivTheme()` **0** transitions run, bypassing the shutter **390** and **263**. `will-change`: zero in the tree. **The audit itself is the deliverable: 280 pages walked twice in one browser, every element plus its `::before` and `::after`, 5826 moving elements, and 0 above 1ms at `reduce` - measured with nothing underneath.** The 96 it found first were in five places and every one is now closed by name: `design/_stand.css`, a stylesheet **loaded by 91 coloured screens and in NEITHER corpus the census walks**, carrying four literal durations invisible to every number the stage had printed; `/_nav.css`; `design/overview.html`, whose declared exception was about the token and never about ignoring a person who asked their system for less motion; `scroll-behavior: smooth`; and **`motion.html` itself - the one page in the tree whose subject is motion was the one that kept moving**, because its demo answered only its own toggle. **And reducing motion cancelled no state**: ten surfaces opened under emulation all arrived (opacity 1 within 30ms, `display` set, duration `0.001s`), and the eleventh, whose state sits on its parent and which the live probe cannot reach, was measured by hand - `1.000` at +30ms against `0.391` without emulation. **The safety net: the owner decided NO**, on this stage's own evidence - the net that was already there cost four stages of numbers, and stage 12 is covered by an instrument instead (`motion.mjs --reduce` is a gate now). Gates: accept **283/0** at 390 · reduce **5826/0** · roles 84/0 · inventory 0 · vars 283/0 · links 5353/0 · paths 1421/0 · bp · theme **0 broken by the theme** · typo · css-comments. **Step 6 ran the critique on two instruments plus a reader, and the reader is what earned the step.** The computed-style table is the main instrument and the GROUPING is what it measures: 280 pages, two viewports, every element plus its pseudo-elements, 360 asserted (`clientWidth` exactly 360 on all 280, and **no page produced a horizontal scrollbar**) rather than intended. **Exactly four durations render in the product - 150, 220, 330, 1100 - and all four are tokens**; grouped by role, **32 files carry RESPONSE and every one renders 150ms and nothing else**, against the seven different numbers the census found doing that one job. The role comes from the «Рух» column of `inventory.md` and not from the number, or the check would be circular - and the table had two attribution bugs of its own, both of which would have printed a role drift that does not exist. **Codex found the hole between the corpora**: `design/_stand.css` is loaded by 91 coloured screens and stands in NEITHER - `system` reads `design/system/**.css`, `screens` reads `design/**.html`, and a stylesheet at the root of `design/` is in neither, so four stages of numbers described a corpus missing a file that moved on ninety-one pages. A `harness` corpus now exists, and **its own filter was wrong on the first writing** - depth 3 instead of 2, so it excluded the only file it was written for and reported «1 file, 0 with motion»: a corpus that looks measured and covers nothing. Codex also found the **last two literal curves** (`visibility 0s linear`), which bought nothing because `visibility` is discrete; measured after removal, the closed drawer still reports `transitionDelay: 0s, 0.33s`. **The reader found three defects no instrument could, and all three live between two files that are each correct alone**: `transform` was missing from the `.btn` transition list while `.btn--lift:hover` sets `translateY(-1px)` two hundred lines below, so half a moment eased and half snapped; the coach's client card had two states and answered neither, because the roll-call's file-level verdict counted the file as «moves» on the strength of a different moment inside it; and **`scroll-behavior: smooth` had been in the product since stage 07 with no job named anywhere**, which by this stage's own rule means it should not exist. It also read the bare «–» in the motion column as a verdict and could not say why - so all **33** bare rows now carry their reason, and the two kinds are told apart. **Its voice is not equal to Codex's**, so agreement would have added nothing and divergence is worth triple; it diverged on three. Withdrawn on verification with reasons: the fade pairs across twelve files (the technique per surface, not a duplicated rule), the em dash inside the sentence that quotes the ban, and the 24 paint-heavy declarations (every trigger is a pointer, focus or a selection class, so one element repaints at a time). Deferred with a reason: the two spinners write the same rule twice, and merging needs one class on markup the frozen corpus writes - stage 12 rebuilds both screens anyway. **`motion.md` gained the slice stage 12 will actually ask for**: `ЕКРАН -> МОМЕНТИ`, produced by `tools/screen-moments.mjs` over the **grey** corpus (142 screens, the whole product, against 92 in colour), because a table covering only what is already coloured is silent about the thing it is written for - 136 screens carry moments of their own, 6 carry only the shared ones and say «руху немає» with the reason. The motion rule is in `architecture.md` section J and in `design/system/CLAUDE.md` **inside rule 7 rather than as an eleventh line**. `pixel-proof.html` carries the stage-11 section: six before/after numbers, the role table, and the critique summarised BY CLASS with a status on every row. Route checked instead of a deploy: **root `index.html` to `motion.html` in one click**, 12 sections on the page. Gates re-run after the repairs: accept **283/0** · reduce **5828 moving, 0 above 1ms** · roles 84/0 · inventory 0 · vars 283/0 · links 5353/0 · paths 1437/0 · bp · typo · css-comments. **Both owner passes ran, and the critique found the defect on the one feature the owner personally chose.** Branch B buys a MECHANISM and inherits its values: read from inside a live navigation, the crossfade ran on **250ms with the curve `ease`** - the exact value the stage removed from 817 of 818 timing functions, back at full size on the largest arrival in the product. The three root pseudo-elements now carry `--dur-slow` with the curve read off the direction (`--ease-exit` out, `--ease-enter` in, `--ease-standard` on the group), 80ms slower than the browser default and said out loud. **The census could not have found it and could only ever have been silent**: it reads computed style off ELEMENTS, and these animations live on a pseudo-element tree that exists only while a navigation is in flight. `tools/motion.mjs --view` is the mode that asks, with **five wrong versions** beside it - three about driving the product through doors a visitor does not have, including a CDP `Page.navigate`, which is a browser-initiated navigation and therefore one of the cases a cross-document transition is specified to SKIP. Falsified twice: comment the override out and it prints five lines at 250ms `ease`; swap `--dur-slow` to 7.77s and all five read 7770ms. **My own first reading said `linear` and was a true reading of the wrong half** - a CSS animation spells its curve on the effect (a default) and on every keyframe (what renders); corrected in the log in place. NOT taken with a measured reason: `view-transition-name` has 0 declarations and element continuity needs names unique per document, so 12 product cards would make Chrome skip the whole transition - a stage-12 order in `backlog.md`. `/impeccable audit`: **15/20** - performance 4, implementation integrity 4, theming 3, accessibility 2 (26 nodes of white on `#FF5A00` at 3.13:1), responsive 2 (**137 text nodes under 11px at 390 against 53 at 1280**, 62 of them `product-card`). Withdrawn on verification: 124 «text-occlusion» at 390 are the stand chrome, 13 «layout-transition» are the three declared ones. `theme.mjs` re-run whole: **27 forms fail, 0 of them broken by the theme** - the debt is older. The third instrument, the contract as a checklist, stands as its own table on `pixel-proof.html`, apart from the defect table: three rows are not green and each says why, and one row that had looked green for two steps was the document transition itself. |
| 12 | Rollout | **Done.** **141 pages in the registry, 140 in colour, 1 left and it is the one the owner put after launch** (the quiz). Eighteen subagents over five batches assembled 49 screens against ONE contract held in a file and substituted five values per launch; the whole MVP scope is in colour. **The stage's finding about itself is a proportion:** of the 123 questions the fan-out produced, **62 are defects of the INSTRUMENTS, not of the screens** - eleven green counters that could not see their own class (`links`, `rollout-table` and its own summary line, `typo`'s verdict and its extension list, `dead-sel`, `screen-css`'s declared count, `bp`'s excused array, `glyphs`, `paths` excusing two files this stage BUILT, `key-alpha` whose transform existed with no check under it, **`theme`, whose three `visit()` calls hard-coded 1280 so «0 broken by the theme» meant «0 at 1280»**, and the coverage map itself, which named 54 of 141 pages and could not look incomplete). **The contract's tenth mark did not exist**: «плюс клас, якого немає в системі» had been in the ban list since batch 1 and in no instrument, so five batches of «чисто» answered nine tenths of a sentence; written at step 7 it found 24 names on 101 places, 23 inert and one a real lost rule. Two new instruments (`coverage.mjs`, `key-alpha.py --check`), eleven repaired. Critique on two instruments at step 7: Codex read-only over the source, three Claude agents in a browser over 80 screens at a measured 360 in both themes; four findings withdrawn on verification with the reason written into the file. Gates at close: `accept` 341/0 at 390 and at 360 · `screen-css` clean on all ten marks · `vars` 341/0 · `links` 6049 with 0 dead · `dead-sel` 3386 selectors 0 dead · `typo` clean with the parse gate now covering every kind it rewrites · `paths` 1556/0 · `coverage` 141 records / 141 opened / 50 of 50 with their own states / one panel on 141 of 141 · `bp` clean · **`wireframes/` 0 files changed for the whole stage**. **12.11 - BATCH 6 RAN ON THE OWNER'S WORD, AND THE COUNT IS 141 OF 141.** The quiz (node 4.x) is in colour, `OUT_OF_SCOPE` in `coverage.mjs` is an EMPTY declared list, and the coverage map is green on every page for the first time. The round cost one component (`quiz.css`), one badge form (`.tag-base`), one card rung (`.qsc`) and a brow unified out of four editions into two weights - and **25 of the 59 names the grey screen carried were READ off the ladder rather than drawn twice**. **The stage's last red gate is closed:** `idle.mjs` went from 67 declarations on 29 pages to **0 on 94**, and the books close exactly - 10 of the 67 were the instrument (a file extension read as a class, two classes that land on `html`/`body`, and 7 the reverse question could not see because it asked only ONE of the page's two lists), the other 57 were real and were paid with 57 live demos on 26 stand pages, «винне демо» at 0 throughout. **The one defect the batch found stood on a step behind a click:** the quiz footer drove the document 68px sideways at 360, and `accept`, `split` and `width-sweep` all called it clean - the last of them 129 times. `tools/steps.mjs` is the instrument that asks, and its own first edition failed falsification before it worked. Why any of it is the way it is: `docs/decisions.md`. |
| 13 | Handoff | Not started |

This table and `done:` in `/_nav.js` are the only two places status is written. `CLAUDE.md` holds
rules and never a status.

## The route to the end

**Corrected 2026-08-13, and the correction is the point.** The route written here that morning was
**invented**, not read out of the pipeline packs: it claimed the dark theme had to wait for a
"tirage" and that stage 08 owed steps 6, 7 and 8 in a made-up order. The packs in
`AI Design Workflow/` say otherwise, and they are the source of truth.

- **The dark theme is stage 08 step 7**, and it is a **stress test of the system**, not decoration
  at the end. It goes in early precisely because it breaks whatever is badly separated, and early
  is when that is cheap to fix. `08 - Tokens Components.md`: «пара тем обов'язкова з першого рядка
  ... роль без пари не існує». Built 2026-08-13.
- **The rollout is stage 12**, after Responsive (10) and Animation (11). A screen is assembled
  **once**, from a system that is finished; a component the rollout would lack has to be found
  before it drags states, a pattern, breakpoints and motion behind it.
- Stage 08's real steps 6, 8 and 9 are «звід системи з продуктом», «переїзд вибірки (піксельний
  доказ)» and «перевірка і фінал» - not what the task list paraphrased them into.

The withdrawn route is left visible on `design/kit/why.html` beside the real one, with the reason.
A route corrected silently comes back in the same words.

## Repository

```
_nav.js  _nav.css  index.html    roadmap registry, its look, and the entry point
research/     stages 01, 02, 02+   research, competitors, benchmark, AARRR, UX patterns,
                                   personas, JTBD, CJM
ia/           stage 03             base layer (flows, concept map) + detail layer
                                   (sitemap, structure, per-node specs)
wireframes/   stage 04             grey clickable prototype, frozen after Voice
voice/        stage 05             voice rulebook + microcopy inventory
design/       stages 06-09         concept/, colour theme, kit/, system/, product screens
docs/         decisions.md (why anything is the way it is) + playbook/
tools/        the checks a step runs before it says done - see tools/README.md
```

Each stage folder holds md sources of truth in `docs/` and its html visualization flat in the root.
`index.html` is always the home page of the folder you opened; a hub is always `overview.html`.

**Before any step says done:** `node tools/accept.mjs` (every screen at 390 - overflow, console
error, em dash, curly apostrophe, doubled crumb), `node tools/css-comments.mjs` (every stylesheet
in a second - CSS is silent about an orphaned comment and it once reached a screen), and
`node tools/states.mjs` when the step touched a dialog, a drawer or an overlay. No server to start
and no page list to type: each finds its own.

## The design system

The system is a product of its own, with three readers and one entrance for each of them. It is live
on GitHub Pages together with the rest of the repository; nothing here needs a separate deploy.

- **[Why it is the way it is](design/kit/why.html)** - the guide. Where the visual language came
  from, how the values reached the tokens, how to use the system and how to grow it. Start here if
  you have never seen this system before.
- **[The showcase](design/kit/overview.html)** - all 84 components as cards, grouped by the ladder of
  levels, each with its own page: anatomy, variants, when to use, rule and anti-rule, states in both
  themes.
- **[Patterns](design/kit/patterns.html)** - the level above a component: stable compositions the
  product already repeats, the rule for when to take one, and the candidates waiting for a third
  screen.
- **[Architecture and the rules](design/kit/architecture.html)** - the decision sheet, the rules of
  use (section I) and the contribution rule (section J).
- **[Backlog](design/kit/backlog.html)** - what the system could not do, and what was found and
  deliberately not fixed.

**The contribution rule, in one line:** new appears in `design/system/` first, then on the screen,
never the other way round. A screen declares no styles of its own; what it lacks is an order for the
system. Ten lines of it live in the code, in `design/system/CLAUDE.md`.

**Two clicks from the root**: the sidebar of any page is the roadmap registry, and «Дизайн-система ->
Чому саме так» is the row that opens the guide.

## Key documents

- [CLAUDE.md](CLAUDE.md) - project rules in force (200-line budget)
- [AGENTS.md](AGENTS.md) - entry point for an external reviewer
- [docs/decisions.md](docs/decisions.md) - decision records and the build journal
- [DESIGN-artifacts.md](DESIGN-artifacts.md) - visual language and the origin of every value
- Sources of truth: [`research/docs/`](research/docs/) · [`ia/docs/`](ia/docs/) ·
  [`wireframes/docs/`](wireframes/docs/) · [`voice/docs/`](voice/docs/)
