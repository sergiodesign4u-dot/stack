# Stack

A mobile-first sport nutrition store for the Ukrainian market, built around coaches and gyms who
order for their athletes. Turns a confusing catalog into a clear, trusted path from a person's goal
to the right products.

**Entry point:** https://sergiodesign4u-dot.github.io/stack/ - the project route with every stage and
its status. Every page carries the same left sidebar, rendered from one registry (`/_nav.js`), so
there is no page list to maintain here.

## Status

| # | Stage | Status |
|---|-------|--------|
| 01 | Foundation Research | Done |
| 02 | User Research (personas + JTBD) | Done |
| 02+ | CJM (As-Is + To-Be) | Done - Olena x main job, emotions sourced live; added the order status notification to the MVP |
| 03 | Information architecture | Done - base layer, detail layer, node hub and block bank |
| 04 | Wireframes | Done - grey clickable prototype, 141 screens + hub |
| 05 | Voice | Done - rulebook + microcopy inventory, rolled out to the prototype |
| 06 | Concept | Done - visual language on two screens |
| 07 | UI + Visual | Done - component kit and inventory; the coloured layer is 87 screens of 141 plus the stage hub, counted 2026-08-12 (46 buyer + 41 coach; A13 brought 8 at step 7.95, step 8.7 added three, step 8.13 two, and step 8.14 the 35 page states the grey layer already drew) |
| 08 | Tokens + Components | In progress - 84 component files, 91 pages in `design/kit/`: the architecture sheet and `pixel-proof.html` among them. The coach flow is in colour and its own navigation no longer leaves it (A13 + step 8.7, 11 screens). Step 8.10 cleared the eight leftovers that needed no owner decision, and `badge.css` gained a second kind - the outlined badge, one shape that had been living under four names in four files. Step 8.12 took four owner decisions: A6's measured half (`product-thumb.css`, one box that was living under two names), A9 closed (the price grid, not the label), and the native checkbox takes the system's look. Step 8.13 measured section C before building and found a quarter of it wrong - 17 of the grey layer's 142 screens are empty states and 25 grey states of already-coloured screens are a clone away, not a design job; «no orders» and «no addresses» are coloured. Step 8.14 cloned the 35 grey page states whose base was already coloured - 48 candidates counted, 13 of them dialogs the coloured screen already opens - and the transform is `tools/clone-to-colour.mjs` now. Step 8.15 built the 10 states that existed nowhere - four new declarations in total, because the atoms were already there - and closed section C. Step 8.17 translated the private `<style>` blocks of 30 coloured screens that were still speaking the grey layer's variable names - an undefined custom property is silent, so `coach-verify-tier` had been drawing white ink on white paper past all four gates (`tools/vars.mjs`, `tools/grey-vars.mjs`). Step 8.18 asked the question no gate had asked: **803 of the 2882 internal links in `design/` went nowhere, against 0 of the 1579 in `wireframes/`** - 650 because the kit stand and its demo sit one directory apart and share their markup, 152 the tirage into the 41 screens still grey-only, 1 the design hub's favicon (`tools/links.mjs`, the fifth check). Step 8.19 closed **A10 by the owner's call** - sub-AA accent text is accepted for this shop, nothing on screen changes - after re-counting, because the 7.81 census read 40 screens and there are 88: the sheet's biggest line (`.badge`, 28 at 2.91) had been ink since 8.10, the `₴` was counted as passing when it is 56 of 62 failures, and **the largest failing shape in the product - `.on` «Українська», 82 of 88 screens - had never been rendered by any census**, because `states.mjs` matched two opener names typed by hand, one of them dead and three real ones missing. **Step 7 done 2026-08-13, and it was overdue**: `[data-theme="dark"]` had never been written, so steps 3 and 5 had each delivered half their contract (the pack: «пара тем обов'язкова з першого рядка... роль без пари не існує»). 85 semantic roles now carry a dark half, 23 dark-side primitives derived by one rule - hue and saturation kept, lightness solved so each colour stands to the dark page as its light twin stands to white. `design/system/theme.js` applies the choice in `<head>`, before the first paint, and the switch is in the stand's panel (step 4's debt). The product's own switch - sun and moon in the top bar, where the owner pointed - went in the same day; the pack puts it at step 8 and its one condition, «коли екрани переїдуть на систему», was already met. **The step's harvest, and the pack says a component file the theme forces you to edit is exactly that**: `--text-oninverse` was declared as «the label on every dark AND every orange fill», two grounds in one role, and the dark theme pulled them apart - the inverse bar flips, `#FF5A00` does not, so every white label on the accent went near-black and every ink label went near-white (`.chip.on` 4.78 -> 2.55, `.tag-new` / `.hb` / `.tbadge` / `.q` 5.45 -> 2.97). Split into `--text-onaction` (white, the owner's 27.07.2026 rule) and `--text-onaction-ink` (5.45, the 7.47 exception), plus `--line-onaction` for the same disease on the LINE surface - the ring around the price knob read **1.36** against its own track in dark. 16 rules across 12 files. **The instrument was wrong three times on the way**, and each wrong version is written into the file beside the right one: a LIGHT column beside the dark one reclassified its own output (final count **48 shapes - 7 the theme broke, 41 fail in both**), a ground painted by a gradient is now said rather than guessed, and the zero-alpha test was rewritten twice - as a regex it first looked for a literal «s» (a lone backslash inside a template literal is eaten before the string is ever a regex), then matched `rgb(255, 90, 0)` and silently deleted every orange word on the stand from the measurement. Both wrong versions produced a SHORTER list, which from a checker reads exactly like progress. `tools/roles.mjs` is the seventh check, born because 12 stand pages went on printing the old token names and nothing asked - it finds **35 of 82 components adrift**, only 12 of them today's, and those 12 are corrected. **Step 7.9 came from the owner opening a product screen in the dark theme**: the left panel that walks the 88 coloured screens was rendering its flow heads, node numbers, arrows and foot links at **1.05** - white on white. The reason nobody had seen it is the whole finding: `theme.mjs` chose its subject with three words («starts with `kit/`»), so it measured **87 pages of 203 and had never opened a single product screen**. Pointed at the home screen for the first time it returned 14 shapes and 42 instances, 32 of them in that panel. `design/_stand.css` paints the frame, which stands on `--bg-inverse` - a role that flips, correctly, because that is how the frame keeps saying «I am not the product» - and it painted **16 raw `rgba(255,255,255,.x)` literals plus one primitive read directly** onto a plate that moves. It was invisible to both source checks by construction: `theme.mjs`'s leak check and `roles.mjs` both read `design/system/components/`, and the frame is neither a component nor a stand page. Nine values became **five paired `--stand-*` variables**, and the pair is not the same alpha with a different ink - it is the alpha that reproduces the light half's contrast on the plate it actually has (.66 white on `#1C1C1C` is 8.02; 8.03 on `#FAF9F7` needs ink at .75). Four labels riding the accent fill still read `--text-body` and went 5.45 -> **2.97**: yesterday's split had a role for them (`--text-onaction-ink`) and the frame was not among the twelve files corrected, because it is not a component. **`--text-onphoto` is deleted** - one reader in the whole system, `hero.css:31`, and its «photograph» was a solid `--bg-inverse` plate declared four lines above, so 17.04 became 1.05; the right role had three readers on that very plate and has four now. **And that role was itself 3.66 in the dark against 8.02 in the light**, because `tokens.css` says «the direction flips, the opacity does not» and it does not hold - `--scrim-ink-75`, 8.03, solved rather than chosen. The instrument also stopped being able to pass silently: it now returns the theme it was actually standing in (a page without `theme.js` swallows the switch without a sound, and the probe then compares a light page with itself and reports a perfect result) and counts the two ways a page leaves a run unmeasured - **199 of 203, and the 4 are named**. Full corpus: **81 shapes, 28 the theme broke, 53 fail in both**, and the widened subject immediately named the next step: **21 shapes on 12 coach screens standing on grounds that stay `#FFFFFF` in the dark theme**, because **25 of the 88 product screens carry a raw colour literal in a private `<style>` block and all 25 are coach screens** - the other 63 carry none. **Step 7.11 came from the owner looking at the dark theme**, and both things he named were right. **«Шоколадная, но какой-то вдруг серый»:** `--bg-surface` pointed at `--warm-900` `#1C1C1C`, which is the brand's **Ink** - H0 S0, a plate pixel, and never a rung of a warm ramp - while the page, the sunken and every hairline around it were H35 S15. So the row that paints the most (every card, the header, the search field, the footer plates) sat neutral grey inside a warm theme. Slot 900 was taken by something that is not a rung, and stage 09 step 1 had left it alone «because the mirror agrees with where it already is» - the mirror was asked about lightness and never about hue. `--warm-880` `#1F1C16`, same rule as its four neighbours, lightness solved for the L\* the grey already stood at (10.41 against 10.27), so **no contrast in the theme moves**; `--grey-ed` `#EDEDED` went the same way as `--warm-150`, the last neutral fill. **Nothing was failing** - both halves present, no primitive leak, contrast right everywhere - which is exactly the class step 7 exists to find. `theme.mjs` check **2b** asks it now: a dark-theme role that paints an AREA and carries no hue, with the exemption written as a rule rather than a list (a role named `-onaction` / `-onink` / `-onphoto` declares its own ground, and those grounds do not move), both exemptions printed, and **proved by putting the defect back and watching it fire**. **«Медведь темный почему-то»:** `mix-blend-mode: multiply` was never a blend decision - `mascot-pose-present.png` is RGB with no alpha, drawn on white, and multiply is the one blend that hides white on a LIGHT plate; on a dark one it multiplies the bear down to the plate's own darkness. Fixed in the asset: white keyed by a flood fill from the border (not «every white pixel» - that punches out the highlight in an eye), the anti-aliased rim keeping its softness via `a = 255 - min(r,g,b)` with the colour un-multiplied out of white. 56.9% transparent, and the file is 30KB smaller. Counted and left open: `multiply` is on **seven** rules and **five PNGs in active use have no alpha across 25 references** - where nothing blends them at all, the two hero promo tiles, the white rectangle is simply visible and is the brightest thing on the dark page. **Step 7.13: the owner sent a picture of what he wanted and it decided three lightnesses.** A histogram over his image gave page `#0B0A08` L\* 2.8 (30.6% of pixels), surface `#181512` L\* 7.0 (35.8%), sunken `#201D15` L\* 10.8, hairline `#2C2822` L\* 16.3 - against 7.4 / 10.4 / 13.1 / 16.4 on screen, so **the dark end goes down and compresses while the hairline stays**. The lightnesses are taken and the hues are not: his three read H40 / H30 / H44, which is what hand-picking one colour at a time does, and this ramp has one hue - re-derived at H36 S16. **Ink finally left the ramp as `--charcoal`**, the name DESIGN-artifacts gives it, because with the surface at L\* 7.0 slot 900 was not just ugly but out of order (850 read 10.9, 880 read 7.0, 900 read 10.3 - a higher number lighter than a lower one). Nine `var()` readers in `tokens.css`, five prose mentions, two census lists - and **no component file changed**, because components read roles and never primitives, which is the claim the two levels were built to make and the first time it has been cashed. Measured cost: the steps in L\* were 5.2 / 3.3 / 2.7 / 3.0 and are 5.2 / 5.5 / 3.9 / 4.1, so a card stands off the page **more** than before (1.09 against 1.06) and the hairline off the surface more too (1.25 against 1.16); the accent went 5.76 / 5.45 / 5.10 / 4.67 -> **6.32 / 5.82 / 5.37 / 4.67**. And the part that would have been easy to skip: **the two dark text rungs had to be re-solved**, because the mirror that makes dark secondary text stand to its grounds as the light one does breaks silently when the grounds move - the old `#AA9D8A` would have read 7.43 / 6.84 / 6.32 against a 6.84 / 6.50 / 6.02 target, the caption louder in dark than in light. `--warm-400` `#A59883`, `--warm-500` `#95856C`, fit ±0.14 where it used to be ±0.08, written into the file rather than rounded away. **Steps 7.14-7.16: the owner walked the dark theme and sent five screenshots, and three of them were one class.** *Images without an alpha channel:* step 7.11's key had left **16402 near-white pixels** in the mascot - background the drawing had sealed off, a 117x196 pocket between the legs and a 26x150 strip between the arm and the body, invisible on white and a gash on dark. `tools/key-alpha.py` now carries three rules, each written because the naive version failed on a real file: the background is a flood fill from the border (a global key punches the highlight out of an eye), **an enclosed pocket is still background** told apart from a highlight by measurement with both counts printed, and **the rim is found by connectivity rather than by a radius** - a 3px band left the drop shadows behind, because a shadow baked for a white page is 20 to 60 pixels wide and read as a pale smear under every mascot. Six assets keyed and checked over `#0C0A08` and `#F2F0ED` before shipping; **all seven `multiply` rules are gone**. *Raw colour in the coach flow:* **119 occurrences of twelve values on 25 screens**, and broken down BY PROPERTY rather than by value every one had exactly one honest role - `background:#fff` -> `--bg-page` (42), `color:#bbb` -> `--mark-faint` (17), `color:#aaa` on the struck price -> `--text-price-was` (8, a role with 0 rendered uses until now), the white step numerals -> `--text-onaction` (5), and so on. The first pass got 117 of 119: **a comment in front of a selector** («/* big primary CTA */\n.cnew») meant the map never saw the key, corrected in the rule and not by hand. Then the full sweep found two shapes still at 1.05 on pure white - in an **inline `style=` attribute**, the one hiding place the census had not read, exactly one declaration wide. *And `dna`:* the same fault `flask` was fixed for - every rung stopped 0.53 to 0.75 SHORT of its strand, so paint overlapped by 1.07 to 1.75 while the geometry never touched, which is a blob and not a junction; the glyph was also 9.9 x 18.9 of ink, the narrowest and tallest in a set whose median is 18.5. Redrawn to 11.9 x 18.9 with all four rungs ending ON the curve, and the four heights read off the width curve rather than spaced by eye. **Corpus: 81 shapes with 28 broken by the theme -> 65 with 5**, and those five are `.hptag` plus four rows of one swatch. **Step 7.17, the owner again: the header's account dropdown was the one region of this header the colour layer had never opened** - five bare rows with no mark, a brand-ink cap between the two accent ones its siblings `.wfh-mega` and `.wfh-langmenu` were given, and a hover that moved the ground and left the word. It takes the account rail's answer now, and the marks are ASKED FOR rather than chosen: `uivCabMarks()` reads them out of `WF_ACC_LINKS` by label, then by destination («Кабінет = Огляд» turned out to be a fact of the markup, not an alias), and only six rows the frozen layer writes by hand need a table, each value copied from the line named beside it. **Two rows also pointed at the wrong section** - «Замовлення» and «Адреси» both went to `account.html` - re-pointed from the same table, which is a value moving and not a decision. The menu knows where you are standing now (`aria-current`, one row on `account-orders`, which three shared destinations had made impossible). Measured by hand in four states and both themes, because **a popup is 0x0 until it is opened and no census sees it** - the second class found that way after `.on` at 8.19, and the argument for widening the theme walk. **Step 7.18, and the owner's sentence is the finding**: «не сильно похоже на дизайн, хотя страница помечена как в дизайне». «Coloured» has meant «every raw hex is a role name» since 8.17, and `vars.mjs` / `grey-vars.mjs` both check exactly that - **no instrument asks whether the role is the RIGHT role**. The grey layer's `--dark #161616` meant «selected / primary» in one value; the move translated it to `--bg-inverse` / `--line-inverse` everywhere, and step 7.96 split the two jobs on `coach-verify.html` ONLY, saying «STILL OUTSTANDING» twice. So the «Стати тренером» flow shipped with its primary action as an ink slab: on `coach-verify-loading` a local `.btn.dark` painted `--bg-inverse` OVER an anchor already carrying `btn--accent`, leaving the atom's orange edge round a black button. **20 declarations on 16 screens counted, 13 screens closed by rule, every fix an answer the system already had** - `.tier-cta` to button.css (the `.cv-cta` move of 7.96), the three spinners to `--line-action` (`.auth-spin` / `.co-spin`), the state discs to auth-dialog's outlined 46px circle in success / danger, the tier cards to `.acard`'s face with `--line-action` on the recommended one, twelve ink rings to badge.css's hairline. On `-tier` the accent appeared once above the fold against five ink slabs; there is one accent fill now, on the action the page recommends. **Seven declarations on five screens are left on purpose** - they are PANELS, and an ink plate is legitimate emphasis here, so ink-or-accent is the owner's look to call. **Step 7.19 closed both open looks by the owner's call**: A10 extends to `.hptag` (nothing on screen changes, seven classes now), and **the product-photo stage does not move with the theme** - five roles paired to themselves plus `--shadow-floor` back to `--scrim-ink-07`, because with the white box gone a black jar had been standing on the two darkest values in the system with an invisible shadow. The mark on that stage had to be frozen too (`--bg-inverse` flips, so a near-white pill landed on a now-white photograph), which is a general statement and not a note about one badge. **That decision rewrote a check**: 2b's exemption was the name pattern `/-on(action|ink|photo)/` and is now the MEASUREMENT «both halves are the same value» - it covers all five roles the pattern did, needs no name remembered, and found `--line-onink` = `--charcoal`, a neutral line on a warm plate, in the same minute. **Step 7.20 re-took the pixel proof and found it could not be re-taken**: re-shooting only the «after» half reported all 40 screens moved by 2-15%, and the diff map showed every glyph outlined twice - the 2026-08-06 set was shot in conditions nobody recorded. `tools/proof.mjs` (the eighth check) now takes BOTH halves from one Chrome, `git archive 9e44109` against the working tree, compares **by pixel** and answers with an **element** via `elementFromPoint`, waiting on the page's own signature rather than a clock (a fixed wait gave two different answers, because `uivBar()` changes the document's height after load). **19 of 40 screens moved on 13.08**, all three causes already named - the photo frames (multiply + alpha), the hero's `.hps`, and the stand's own bar standing in the product's photograph - and the loudest screen that did NOT move measures 0.066%. `overview` measures **0.000%**, being the one screen that is not on the system. **Step 7.21 gave the theme walk the thing it never had: a way to open what it measures.** A popup is 0x0 until somebody opens it and the probe skips anything under 2px, so for as long as this check has existed it read only the part of the product already on screen - a blind spot that had already cost twice, both times found by a person opening a menu rather than by an instrument. Every global matching `open[A-Z]` or `toggle[A-Z]` is called before the probe, all at once (two dialogs overlapping on z do not disturb each other, because a ground is composited from the element's own ancestor chain), and **an opener that leaves the page is discovered rather than listed** - `openCookieSettings()` falls back to a file that does not exist under `design/`, and the first sweep sent every page to a 404 while a check written inside the sweep saw nothing, the navigation being asynchronous. **5890 opener calls over 406 passes**, `--closed` keeps the old behaviour. **Step 7.22 is the owner again, with the account menu open on `product.html`: «почему где-то нет иконок».** `product.html` renders a GUEST header, so logging in makes `wfAuthDone()` rebuild it and the menu that appears has never met any pass: **0 marks of 5**, `🥈` drawn by the font, and both wrong destinations 7.17 had just corrected, back. **The fifth time this shape has arrived**, so the answer is not a fifth wrapper: the chrome's content passes have a name now, `uivChromePaint()`, and `uivObserve()` calls the same one `uivChrome()` does. **Step 7.23**: `coach-verify.css` scoped its stepper under `.coach`, which exactly one of the flow's five screens carries, so the other four each kept a private copy - 1.5px edges against 1px, 11px numerals against 12. `.cv-step` occurs in six files, so the NAME was already the scope; the scope came off seven rules and **28 declarations left the four pages**. **Step 7.24 sent two agents at the step-7 debt and all three of their findings were in the INSTRUMENTS**: `roles.mjs` excluded a self-declaration with `/^\s*--name:/` anchored to a LINE start and so called a correct page wrong (**31 adrift -> 0**); `kit/color` chose its swatch ink by HSL lightness on an unpremultiplied value (`--gold-500` **2.00 -> 8.54**); and **the probe read `color` and ignored `opacity` entirely**, so every reading it had ever printed for a faded element was wrong in the flattering direction. **Step 7.25 re-walked the corpus with that fix and the first thing it returned was not a defect**: `span.tt-m` at **1.00 on 113 screens** is the toast before it is called (`opacity: 0` at rest), and a checker made sharper finds more of everything - noise first. Clean corpus: **79 shapes, 8 broken by the theme, 71 failing in both**, 199 of 203 pages, the 4 named. The same walk closed the coach flow's photo placeholder - «фото» at 8px in `--mark-faint` on `--bg-sunken`, **1.28 to 1.69**, 37 instances - because 8.16 had already settled it for the gallery frame («in the COLOURED layer the placeholder is not faint, it is INVISIBLE») and a chevron is a SHAPE while this is a WORD: `--text-muted`, **5.0**. And `design/kit/screens/` - four full-height shots, 2.7MB, **linked from no html and no md** - got a reader on `pixel-proof.html` rather than a deletion, with «older than the code» said out loud on it. **Step 7.26 took all three owner decisions, and none of them turned out to be a question about the screen it was asked on.** *(a) чорнило:* `--slate-400` could not be lifted by either candidate - 4.41 on `--charcoal`, the darkest INK the system owns, 3.86 on white - and the answer was that **the page was borrowing**. It chose between `--text-onaction-ink` and `--text-onaction`, a pair picked for one true property («this ground does not move») wearing a name that says «the label on the ACTION fill», which a swatch is not. The pair moved to `--stand-swatch-ink` / `--stand-swatch-onink` in `kit/_page.css`, **owned by the stand** - the same call step 7.9 made for the stand frame. The deep half is `--warm-950`, already an ink in everything but name (`--line-onink` reads it): `--slate-400` measures **5.05**, no swatch that passed can fail, and **no new value enters the system**. Declared once with no dark half, by the 7.19 rule - a swatch is the purest case in the repository of a ground that does not move, because it IS the value. *(b) підняти прозорість:* solved rather than argued - `.use` and `.rg` need **.90 to reach 4.62, and .85 gives 4.35**. A fade that has to sit at .9 is a rounding error that costs contrast, so the hierarchy moved onto weight, which costs none. `.sc .st u` went with it: it faded a `<u>` that `card()` has never written. **`kit/color` measures 0 shapes**, and the last one to go was not on the page at all - `.kn-gh i`, the count in the stand's own side panel, at `.7` of `--text-muted`: 2.84 beside a label at 5.05. *(c) акцент:* only `.cnew` is a fill, because it is a single `<a>` wrapping the screen's one main action; `.upsell` + its bar, `.tf-upsell` and `.cs-warn` take `--line-action` on the EDGE and keep `--bg-surface` under it, because **each already holds its own accent button** and the action colour is spent once. `.cs-warn` is written down as the one member differing in kind - it is `role="alert"` and the warning family is the other honest reading. **And the table that would have undone it moved too**: `grey-vars.mjs` still answered `--bg-inverse` for those five selectors, which is not stale documentation but a loaded gun - the next clone would have re-inked them with nobody deciding it. **Four things the work walked into, all one family.** `var(--light)` is **declared nowhere and read in five places**, `--sec` / `--fill` / `--hair2` in six more - all eleven inside `style=` attributes, the hiding place named at 7.16 and never taught to anyone. **`tools/vars.mjs` reported 0 failures over 204 screens while they sat there**, and the sentence in `grey-vars.mjs` calling it «the guard that still matters» was simply wrong: a browser never says «this property has no value», because an undefined custom property makes the declaration invalid at computed-value time and the property takes its inherited or initial value instead. **The real guard is table-free and reads the source**: every `var(--x)` that is read has to be declared - `grey-vars.mjs` check 2, 176 pages, **0**. It found two false positives about itself on the first run (`--space-20` and `--fs-11` inside the comment that tells the story of step 6.10 deleting exactly those two names) and now blanks comments before scanning. Step 7.25 also turned out to have reached only the component files: **13 declarations on eight coach state screens** kept a private copy of the placeholder rule at 1.69, the identical disease as 7.23. And the coach flow's big CTA had **never** rendered as two lines - `.cn-t` and `.cn-s` are spans inside a span, and the frozen layer proves the intent against itself by putting `margin-top: 3px` on an inline box. Repaired in `design/`, left standing in `wireframes/`, which is frozen. **Step 6 of the pack ran on 2026-08-14, and the first thing missing was the instrument.** `census.md` ends with its own sentence - «the script is the artifact, not the table» - and the script was not kept, so «два заміри одним приладом» was impossible until there was one. `tools/census.mjs` is rebuilt **from the written method** (two viewports, in a browser and never by grep, computed style, the control test verbatim) and stays in `tools/`; the 24-form folding is NOT reproduced and that is said rather than tuned. **The opener sweep moved from `theme.mjs` into `lib.mjs`** instead of being typed a second time, and the number it produces reprices every census this project has taken: **35 714 clickable observations become 63 154** with panels open, so **43% of the product's clickable surface is behind a state** - including step 1's own 22 229, taken statically. **List 1: nothing is missing from the system** - two candidates came back and both sat on the hub that lists screens rather than being one; 22 controls are tirage, on the 54 grey-only screens. **List 2 is the body of the work left: 31 screens, 1 154 private rules, 886 of them redrawing a class the system already owns**, and the twelve loudest are all coach screens. It is not cosmetic - `coach-session.css` stacks `.qa-row` at 479 and the base screen passes at 360, while four state screens carry a private copy WITHOUT the media query, so «Додати клієнту» hangs 10px past the viewport and `overflow-x: hidden` CLIPS rather than scrolls: **`accept 360` returns 4 failures over 204 screens**, at 390 and 1280 the same corpus returns 0. **That is also why stage 10 cannot start here** - of 222 media blocks, 52 live in those private blocks and add 7 boundaries of their own. **List 3: zero dead classes remain** - six deleted by the owner, the seventh an error of the instrument; beside them 30 behind a state the walk cannot reach and 43 waiting for their screen. **The instrument was wrong four times and all four errors had one shape**: each answered the question NEXT to the one asked and returned a plausible number - 962 dead from reading only control rows, `svg`/`jpg`/`png`/`html` from the tails of `url()`, twelve overlay classes from opening nothing, `menu-pop` from skipping `display:none`. **The fourth cost the 35-minute walk twice, and not because of the bug**: the correction was announced before it was verified, the patch had never applied, and the re-run returned a byte-identical record - 1518 distinct classes, 50 417 total, in both files. The identity is what exposed it. **Three of the six deletions were not lint** but designed states nobody had applied (`certthumb--pending`, `qans--wait`, `tsx--unproven`); their reasoning is verbatim in `backlog.md` so re-adding any is one line, and five stand pages were edited the same step because a stand that demos a class the system no longer has is «worse than no stand». `design/kit/backlog.html` is the step's output and its 88th page. **The migration's first half was then measured, applied and reverted by its own proof.** `tools/private-css.mjs` (the ninth check) deletes a rule and asks the browser whether anything moved: **262 of 1 185 top-level private rules are inert** - **215 match no element on their own page** (clone residue: `clone-to-colour.mjs` copies a full screen's block into its empty and loading states whole, `coach-order-loading` is 37 of 42) and **47 match and move no value**, which is the smaller kind and the telling one - the private blocks do not duplicate the system, they **draw instead of it**. **The cut shipped and `tools/tree-diff.mjs` (the tenth) caught it**: git archive the reference into its own tree, two servers, one Chrome, compare the computed style of every element on 40 properties at both widths - **9 movements on 5 screens**, a rail link orange at 390 and a page 555px taller at 1280. All reverted, re-check 0. **The cause is the method: inertness is not additive.** Each rule was tested ALONE with every neighbour in place; `coach-clients-cap` held `.acc-nav` and `.acc-link[aria-current="page"]`, neither alone changed anything, and losing both turned the rail into the mobile chip strip whose current chip `account-shell.css` paints `--bg-action` under `@media (max-width: 959px)`. Two corrections went in - a cumulative walk against a **full-document** snapshot (a rule restyles only what it matches, but LAYOUT travels) and re-offering one width's set to the other (their **union was never tested anywhere**) - and neither closed the gap, so the deletion waits and the proof stays the gate. **Five more instrument faults on the way, all one shape**: a probe that answered «0 rules» about a block of 77 (a swallowing `try`), a comparator that hung with no output, one that reported its own two server PORTS as a difference, one whose regex literal was eaten by the template literal holding it, and an argv off-by-one that made a PAGE the git ref. Each is written into its file beside the fix. Next: a probe whose verdict the proof agrees with, then the 262; the 210 local declarations; the 105 dead `class="dark"` (a rule in `clone-to-colour.mjs` plus a sweep); `.cline.oos` at `opacity: .5` (a look, not a bug); and the pack's step 9. **Steps 8.20-8.31 walked the remaining private rules one by one**, 468 -> 138, and pile 1 of item 3 is CLOSED (the three that remain are `.cv-card`, deferred to stage 09 by decision); **two components were born out of them rather than split out of a stylesheet**: `upsell.css` (the Pro panel two screens drew under two names, one of them citing the other in its own comment) and `plan-card.css` (the Free / Pro card three screens drew under two name sets, both of which had typed out the face of `.acard` by hand while naming that atom in the sentence above the copy). Three live defects the passes found by measuring rather than reading: `.cv-card` laying three state screens out in a ROW on every desktop, a cancelled phone chip strip on four cabinet screens, and `btn--accent` on «Скасувати підписку» - the orange fill on the control that ends a paid subscription. **And the inventory turned out to list none of the eight coach components**, which is written into `design/kit/docs/inventory.md` under its own table |
| 09 | Design System | In progress - step 1 done 2026-08-13: **the dark end of the warm ramp**. The scale had eight rungs and no surface between lightness 34 and 11, so a dark theme had exactly one value to stand on; it now has twelve. `--warm-950` `#191612`, `--warm-850` `#26211B`, `--warm-800` `#2D2821`, `--warm-750` `#39332A` - hue and saturation from `#F2F0ED`, the plate's locked Warm Neutral, lightness from the light end's own steps in L\* (2.1 / 3.1 / 3.2 / 5.0), and `#1C1C1C` unchanged because the mirror agrees with where it already is. Primitives only at the time; stage 08 step 7 now reads them. Measured on the way: **the accent clears full AA on every dark rung a word can sit on** (5.76 / 5.45 / 5.10 / 4.67), so A10's sub-AA compromise is a property of the pale grounds and is not inherited by the dark theme. Step 09.1b built a preview page to show it - **withdrawn and deleted 2026-08-13**, because the real theme replaced it; what it found before that was worth the step: **the two-level text hierarchy goes almost flat** (light 16.2 / 6.5, dark 16.2 / 12.1), because `--text-secondary` and `--text-muted` had no dark rung. Step 09.1c took those two: **`--warm-400` `#AA9D8A` and `--warm-500` `#9A8B73`** - the two numbers the ramp never had, and the hole they close is exactly where dark secondary text lives. The mirror lands to the hundredth on all three grounds (secondary light 6.84 / 6.50 / 6.02 against dark 6.78 / 6.41 / 6.00). Step 09.2 wrote **`design/kit/why.html`**, the page the registry owed. **Both 09.1 and 09.2 were taken off the pack** - stage 09's real step 1 is «патерни і заборони з екранів» and `why.html` is its step 3. The palette work stands (a dark theme needed it), the route the page drew did not and is corrected on the page itself. Next: stage 09 step 1 for real |
| 10 | Responsive | Not started |
| 11 | Animation | Not started |
| 12 | Handoff | Not started |

This table and `done:` in `/_nav.js` are the only two places status is written. `CLAUDE.md` holds
rules and never a status.

## The route to the end

**Corrected 2026-08-13, and the correction is the point.** The route written here that morning was
**invented**, not read out of the pipeline packs: it claimed the dark theme had to wait for a
"tirage" and that stage 08 owed steps 6, 7 and 8 in a made-up order. The packs in
`AI Design Workflow/` say otherwise, and they are the source of truth.

- **The dark theme is stage 08 step 7**, and it is a **stress test of the system**, not decoration
  at the end. It goes in early precisely because it breaks whatever is badly separated, and early
  is when that is cheap to fix. `08 - Tokens Components.md`: «пара тем обов'язкова з першого рядка
  ... роль без пари не існує». Built 2026-08-13.
- **The rollout is stage 12**, after Responsive (10) and Animation (11). A screen is assembled
  **once**, from a system that is finished; a component the rollout would lack has to be found
  before it drags states, a pattern, breakpoints and motion behind it.
- Stage 08's real steps 6, 8 and 9 are «звід системи з продуктом», «переїзд вибірки (піксельний
  доказ)» and «перевірка і фінал» - not what the task list paraphrased them into.

The withdrawn route is left visible on `design/kit/why.html` beside the real one, with the reason.
A route corrected silently comes back in the same words.

## Repository

```
_nav.js  _nav.css  index.html    roadmap registry, its look, and the entry point
research/     stages 01, 02, 02+   research, competitors, benchmark, AARRR, UX patterns,
                                   personas, JTBD, CJM
ia/           stage 03             base layer (flows, concept map) + detail layer
                                   (sitemap, structure, per-node specs)
wireframes/   stage 04             grey clickable prototype, frozen after Voice
voice/        stage 05             voice rulebook + microcopy inventory
design/       stages 06-09         concept/, colour theme, kit/, system/, product screens
docs/         decisions.md (why anything is the way it is) + playbook/
tools/        the checks a step runs before it says done - see tools/README.md
```

Each stage folder holds md sources of truth in `docs/` and its html visualization flat in the root.
`index.html` is always the home page of the folder you opened; a hub is always `overview.html`.

**Before any step says done:** `node tools/accept.mjs` (every screen at 390 - overflow, console
error, em dash, curly apostrophe, doubled crumb), `node tools/css-comments.mjs` (every stylesheet
in a second - CSS is silent about an orphaned comment and it once reached a screen), and
`node tools/states.mjs` when the step touched a dialog, a drawer or an overlay. No server to start
and no page list to type: each finds its own.

## Key documents

- [CLAUDE.md](CLAUDE.md) - project rules in force (200-line budget)
- [AGENTS.md](AGENTS.md) - entry point for an external reviewer
- [docs/decisions.md](docs/decisions.md) - decision records and the build journal
- [DESIGN-artifacts.md](DESIGN-artifacts.md) - visual language and the origin of every value
- Sources of truth: [`research/docs/`](research/docs/) · [`ia/docs/`](ia/docs/) ·
  [`wireframes/docs/`](wireframes/docs/) · [`voice/docs/`](voice/docs/)
